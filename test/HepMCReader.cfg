process PROD = 
{

# you can run cmsRun with PGunHepMCWriterRCP on input,
# an example ASCII file TestPGunHepMC.dat will be produced

# you can read it in with MCFileSource
#    source = MCFileSource
#    {
# TestPGunHepMC.dat will be produced 
# if you run cmsRun with PGunHepMCWriterRCP on input
#       untracked string fileName = "TestPGunHepMC.dat"
#       untracked int32  maxEvents = 5
#    }    

# You can also write an "IOAnalyzer" *deriving from EDAnalyzer)
# ( or you can take an example one :
# setenv CVSROOT :pserver:anonymous@cdcvs.fnal.gov:/cvs/lpc
# cvs login (pass : lpc)
# cvs co -d UserExamples_CMSSW home/yarba_j/UserExamples_CMSSW
# cd UserExamples/TryIOMC/test 
# look for IOAnalyzer (.h & .cc)
# Once you build (scramv1 b -r), make sure you have it in your 
# system (SealPluginRefresh)
#    module Analysis = IOAnalyzer
#    {
#       string  MyName="TestIOAnalyzer"
#       PSet ProvenanceLookup =
#       {
#          bool GetAllProvenances = true
#          bool PrintProvenanceInfo = true
#       }        
#    }    

# you can output HepMCProduct (particles) into ROOT file
#    module Out = PoolOutputModule { string fileName = "test.root" }    

# if you want this(these) option(s), make sure you include it(them) 
# in the path 
#    path p = { Analysis, Out }

}
