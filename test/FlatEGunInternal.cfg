process PROD = 
{

# this will run plig-in energy-flat random particle gun
# and puts particles (HepMCPRoduct) into edm::Event 

    source = FlatRandomEGunSource 
    { 
      untracked int32 maxEvents = 5 
      PSet PGunParameters =
      {
	 # you can request more than 1 particle
	 #vint32  PartID = {211,11}
	 vint32 PartID = {211} 
	 double MinEta = 0.
	 double MaxEta = 0.5
	 double MinPhi = 0.
	 double MaxPhi = 360.
	 double MinE   = 9.99
	 double MaxE   = 10.01
      }
    }
    
# you can write your particles (HEpMCProduct) out into a ROOT file;
# if you want the i/o option, make sure you include it in the path 
#
    module Out = PoolOutputModule { string fileName = "test.root" }    
    path p = { Out }

}
